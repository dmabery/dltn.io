const LinkedTagButton = ({ btnText }) => {
    return (
        <span className=" text-slate-500 grow-0 flex-none inline text-xs px-2 rounded-lg py-0.5 mr-1 transition-all">{btnText}</span>
    )
};

export default LinkedTagButton;