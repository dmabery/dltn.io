media: media
content:
  - name: pages
    label: Pages
    type: collection
    path: words/posts
    fields:
      - name: Title
        type: string
      - name: Slug
        type: string
      - name: tags
        type: select
        options:
          multiple: true
      - name: date
        type: date
        options:
        format: yyyy-MM-dd
       - name: body
        type: rich-text
        